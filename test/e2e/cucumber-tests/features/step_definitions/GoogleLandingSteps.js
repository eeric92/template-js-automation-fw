import { Given } from "@cucumber/cucumber";
const { When, Then } = require("@cucumber/cucumber");

Given(/^the browser is opened and navigate to '(.*)'$/, function () {});

When(/^the user introduces '(.*)' in the search bar$/, function () {});

Then(/^the tab with the proper values is displayed$/, function () {});
